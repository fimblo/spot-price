
## Installation

Before running the script, ensure you have SQLite installed. You can install it using the following command:

- Debian/Ubuntu: `sudo apt-get install sqlite3`
- MacOs: `brew install sqlite3`

After installation, run `setup-database.sh` to create the database and tables.
